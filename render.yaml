services:
  - type: web
    name: exohabitai-samridhigupta
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.app_deploy:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0